# Database URL (only needed if USE_DATABASE is set to true)
DATABASE_URL=postgresql://user:password@localhost:5432/bing_search

# Optional: Set to true to use PostgreSQL database, or false to use keywords.txt directly.
# Defaults to true if DATABASE_URL is set, otherwise false.
USE_DATABASE=false